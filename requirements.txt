# =============================================================================
# Financial Sentiment Analysis — Python Dependencies
# =============================================================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# PyTorch: CPU-only build via --extra-index-url saves ~1.5 GB vs CUDA.
# On Streamlit Cloud this is critical for staying within memory limits.
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Data handling ---
numpy
pandas

# --- Machine Learning ---
torch
transformers
scipy
xgboost                   # Gradient boosting for return prediction
scikit-learn              # ML utilities (metrics, preprocessing)

# --- Financial data ---
yfinance                  # Yahoo Finance API wrapper (stock prices)

# --- News data ---
requests                  # HTTP requests (Alpha Vantage news API)
feedparser                # RSS feed parsing for live news pipeline

# --- Environment management ---
python-dotenv             # Load API keys from .env file

# --- Dashboard ---
streamlit>=1.45.0         # Interactive web dashboard (>=1.45 for Altair v5 compat)
altair>=5,<6              # Vega-Lite charts — must match Streamlit's expected API
plotly                    # Interactive charts for Streamlit